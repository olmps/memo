import 'package:flutter/material.dart';

class UpdateCollectionMemos extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.blue, child: const Center(child: Text('Memos')));
  }
}
