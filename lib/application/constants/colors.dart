// Non-themed colors (meaning, always the same, no matter the current theme)
import 'package:flutter/rendering.dart';

const List<Color> collectionCardGradient = [Color.fromRGBO(250, 27, 255, 0.2), Color.fromRGBO(67, 77, 203, 0.2)];
