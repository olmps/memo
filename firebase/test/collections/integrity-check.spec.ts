describe("Collections Integrity Check", () => {
  // TODO(matuella):
  // TEST 1: Maintain naming + id consistency
  // TEST 2: No duplicate memos ids amongst each collection
  // TEST 3 (?): should we check for "data consistency", such as at least 1 contributor, req. description, req. locale, etc.
});
